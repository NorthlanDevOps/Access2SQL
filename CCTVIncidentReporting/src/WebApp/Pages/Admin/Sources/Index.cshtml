@page
@using WebApp.Pages.Admin.Sources
@model SourcesIndexModel
@{
    ViewData["Title"] = "Manage Sources";
}

<div class="page-header">
    <div class="container-wide">
        <h1>Sources</h1>
        <p class="subtitle">Manage incident sources and reporting methods</p>
    </div>
</div>

<main class="container-wide">
    <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 24px;">
        <div style="flex: 1;">
            <form method="get" style="display: flex; gap: 12px;">
                <input type="search" name="searchString" placeholder="Search sources..." 
                       value="@Model.SearchString" style="flex: 1; max-width: 300px;" />
                <button type="submit" class="btn btn-primary">Search</button>
                @if (!string.IsNullOrEmpty(Model.SearchString))
                {
                    <a href="/Admin/Sources/Index" class="btn">Clear</a>
                }
            </form>
        </div>
        <a href="/Admin/Sources/Create" class="btn btn-primary">+ New Source</a>
    </div>

    @if (Model.Sources != null && Model.Sources.Any())
    {
        <div class="table-container">
            <table>
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>Code</th>
                        <th>Status</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var source in Model.Sources)
                    {
                        <tr>
                            <td>@source.Name</td>
                            <td>@(source.Code ?? "-")</td>
                            <td>
                                <span class="status-badge @(source.Active ? "active" : "inactive")">
                                    @(source.Active ? "Active" : "Inactive")
                                </span>
                            </td>
                            <td>
                                <div style="display: flex; gap: 8px;">
                                    <a href="/Admin/Sources/Edit?id=@source.Id" class="btn btn-sm">Edit</a>
                                    <a href="/Admin/Sources/Index?handler=delete&id=@source.Id" class="btn btn-sm btn-danger" 
                                       onclick="return confirm('Are you sure you want to delete this source?');">Delete</a>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
    else
    {
        <div style="padding: 40px; text-align: center; color: #666;">
            <p>No sources found. <a href="/Admin/Sources/Create">Create one</a></p>
        </div>
    }
</main>
